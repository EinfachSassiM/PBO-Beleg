import { FilterStakeholderPipe } from './filter-stakeholder.pipe';

describe('FilterStakeholderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterStakeholderPipe();
    expect(pipe).toBeTruthy();
  });
});
